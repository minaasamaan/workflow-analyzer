package com.mycompany.analyzer.importer.errors.data;

public enum ParsingErrorType {
        INVALID_RECORD_START,
        MISSING_RECORD_START,
        INVALID_RECORD_END,
        MISSING_RECORD_END,
        INVALID_TOKEN,
        INVALID_FIELD_TYPE
    }
